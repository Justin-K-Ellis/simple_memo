export default function Navbar() {
  return (
    <header>
      <nav className="navbar">
        <h1>Simple Memo</h1>
        <div className="login-btns">
          <button>Login</button>
          <button>Sign Up</button>
        </div>
      </nav>
    </header>
  );
}
